[package]
name = "zeekpay"
version.workspace = true
edition.workspace = true
license.workspace = true

[lib]
crate-type = ["cdylib", "rlib"]

[dependencies]
# soroban-sdk added in the soroban-contract feature.
