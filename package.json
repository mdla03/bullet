{
  "name": "bullet",
  "version": "0.0.0",
  "private": true,
  "description": "Bullet: ZK-private payment rail on Stellar. Pay an X handle or email without exposing the sender<->recipient link on-chain.",
  "packageManager": "pnpm@9.7.0",
  "engines": {
    "node": ">=20",
    "pnpm": ">=9"
  },
  "scripts": {
    "dev": "pnpm --parallel --filter \"@zeekpay/backend\" --filter \"@bullet/frontend\" dev",
    "build": "pnpm -r build",
    "build:circuits": "bash circuits/scripts/build-claim.sh",
    "deploy:testnet": "echo \"[placeholder] wired up in the soroban-contract / e2e-demo features\""
  }
}
