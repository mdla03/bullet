{
  "name": "@zeekpay/circuits",
  "version": "0.0.0",
  "private": true,
  "description": "ZeekPay Circom circuits + snarkjs tooling.",
  "type": "module",
  "scripts": {
    "bench": "bash scripts/run-benchmark.sh",
    "pretest": "bash scripts/build-jubjub-tests.sh",
    "test": "node --test test/jubjub.test.mjs"
  },
  "dependencies": {
    "snarkjs": "0.7.5"
  },
  "devDependencies": {
    "circomlib": "2.0.5",
    "@noble/curves": "1.9.7"
  }
}
